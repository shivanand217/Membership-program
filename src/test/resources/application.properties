# Test overrides. The scheduled lifecycle sweep is disabled so tests drive it deterministically
# via SubscriptionExpirySweep.sweepOnce(); everything else inherits application.yml.
membership.expiry.enabled=false
